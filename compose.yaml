services:
  portfolio-backend:
    build: ./backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: "3000"
      # Dans Docker, MongoDB est joignable via le nom du service — pas localhost
      MONGODB_URL: mongodb://${MONGO_INITDB_ROOT_USERNAME}:${MONGO_INITDB_ROOT_PASSWORD}@database:27017/${MONGO_INITDB_DATABASE}?authSource=${MONGO_INITDB_DATABASE}
    expose:
      - "3000"
    networks:
      - web
      - portfolio
    depends_on:
      - database
  database:
    image: mongo
    ports:
      - 27018:27017
    command:
      - --auth
    env_file:
      - db.env
    volumes:
      - ./data:/data/db
      - ./mongo-init.js:/docker-entrypoint-initdb.d/mongo-init.js:ro
    restart: unless-stopped
    networks:
      - portfolio

networks:
  portfolio:
  web:
    name: pangolin
    external: true
